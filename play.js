const name = 'max';
let age = 29;
const hasHobbies = true;



age  = 30;

const SummarizeUser =(userName , user age , userHasHobby) => {
return (
    'Name is ' +
    userName +
    ',age is ' +
    userAge +
    'and the user has hobbies: ' +
    userhashobby
);
};

const add =(a,b)

oonsole.log(SummarizeUser(name, age, hasHobbies))